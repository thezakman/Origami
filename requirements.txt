# Origami runtime dependencies.
# Source of truth is pyproject.toml — this mirror exists for `pip install -r`.
# Preferred install: `pip install .` (or `pip install -e .` for development).

httpx>=0.27          # async HTTP engine
PyYAML>=6            # fingerprint knowledge base (overlay + ingested rules)
rich>=13             # live terminal UI (degrades to plain text if absent)
mmh3>=4              # favicon MurmurHash3 fingerprint

# Optional extra (enables --http2 via ALPN):  pip install "origami-scanner[http2]"
# h2>=4
